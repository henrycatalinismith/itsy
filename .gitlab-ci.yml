---

stages:
  - deploy

deploy:studio:
  stage: deploy
  image: highvalleysystems/itsy:latest
  only:
    changes:
      - editor/**/*
      - itsy/**/*
      - manual/**/*
      - studio/**/*
  before_script:
    - echo fs.inotify.max_user_instances=524288 | tee -a /etc/sysctl.conf && sysctl -p
    - echo fs.inotify.max_user_watches=524288   | tee -a /etc/sysctl.conf && sysctl -p
    - echo fs.inotify.max_queued_events=524288  | tee -a /etc/sysctl.conf && sysctl -p
  script:
    - cd itsy
    - yarn --pure-lockfile
    - make itsy
    - cd ..
    - cd editor
    - yarn --pure-lockfile
    - yarn build
    - cd ..
    - cp editor/webviews/index.html studio/assets/webviews/editor.html
    - cd manual
    - yarn --pure-lockfile
    - yarn build
    - cd ..
    - cp manual/webviews/index.html studio/assets/webviews/manual.html
    - cd studio
    - yarn --pure-lockfile
    - yarn expo login -u $EXPO_USERNAME -p $EXPO_PASSWORD
    - yarn expo publish --non-interactive
  
deploy:website:
  stage: deploy
  image: node:12-stretch
  before_script:
    - cd website
  only:
    changes:
      - website/**/*
  script:
    - yarn --pure-lockfile
    - yarn gatsby build
    - yarn netlify deploy --prod --dir=public --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN
